#include<stdio.h>
int main()
{
  int b[10],temp;
  int i;
  for(i=0;i<10;i++)
  {
    scanf("%d",&b[i]);
  }
  temp=a[0];
  for(i=0;i<10;i++)
  {
    if(a[0]>b[i])
    {
      temp=b[i]);
    }
    printf("the largest number is %d",temp);
  }
}
